<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/view_technician_schedule_back"
        android:layout_width="190dp"
        android:layout_height="72dp"
        android:backgroundTint="@color/orange"
        android:shadowColor="@color/orange"
        android:text="@string/back"
        android:textColor="#FFFFFF"
        android:visibility="visible"
        app:iconTint="@color/orange"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/view_technician_schedule_view" />

    <TextView
        android:id="@+id/view_technician_schedule_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:fontFamily="sans-serif"
        android:text="@string/schedule"
        android:textColor="@color/black"
        android:textSize="30sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/view_technician_schedule_view"
        android:layout_width="355dp"
        android:layout_height="483dp"
        app:layout_constraintBottom_toTopOf="@+id/view_technician_schedule_back"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/view_technician_schedule_title" />

</androidx.constraintlayout.widget.ConstraintLayout>